---
id: cookie-consent
title: Cookie Consent
---

By default, a cookie consent prompt will appear to ask the user whether they accept cookies before any of the agent's analytics scripts will be included on the page.

If you need to disable this, you can do so with the following config:

```js
Homeflow.set('disable_cookie_consent', true);
```
