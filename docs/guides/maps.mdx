---
id: maps
title: Maps
---

Add a configuration setting to declare which type of layer to use on Leaflet maps. The default is the normal leaflet map layer.

```js
Homeflow.set('leaflet_layer', 'google');
```

If you use `google`, make sure the agency has a `google_maps_api_key` set up in their admin area.

If you need to render a custom layer, you can instead pass a callback function:

```js
Homeflow.set('leaflet_layer', function() {
  return new L.tileLayer('https://{s}.google.com/vt/lyrs=m&x={x}&y={y}&z={z}&hl=en', {
      maxZoom: 20,
      subdomains:['mt0','mt1','mt2','mt3']
  })
});
```
